= form_for [@player, @rental_equipment], html: { class: 'form-horizontal' } do |f|
  - if @rental_equipment.errors.any?
    .alert.alert-danger{role: 'alert'}
      %ul
        - @rental_equipment.errors.full_messages.each do |msg|
          %li
            %span.glyphicon.glyphicon-exclamation-sign
            = msg
  .form-group
    .btn-group{data: {toggle: 'buttons'}}
      - RentalEquipment::TYPES.each do |type|
        %label.btn.btn-primary{class: "#{'active' if type == @rental_equipment.type}"}
          = f.radio_button :type, type
          = type
  .form-group{class: "#{'has-error' if @rental_equipment.errors[:brand].present?}"}
    = f.label :brand, :class => 'control-label'
    .controls
      = f.text_field :brand, :class => 'form-control', maxlength: 100
  .form-group{class: "#{'has-error' if @rental_equipment.errors[:size].present?}"}
    = f.label :size, :class => 'control-label'
    .controls
      = f.text_field :size, :class => 'form-control', maxlength: 100
  .form-group{class: "#{'has-error' if @rental_equipment.errors[:rental_date].present?}"}
    = f.label :rental_date, :class => 'control-label'
    .controls
      = f.date_field :rental_date, :class => 'form-control', maxlength: 100
  .form-group{class: "#{'has-error' if @rental_equipment.errors[:return_date].present?}"}
    = f.label :return_date, :class => 'control-label'
    .controls
      = f.date_field :return_date, :class => 'form-control', maxlength: 100
  .form-group
    - if can?(:edit, @rental_equipment)
      = f.submit t('rental_equipments.actions.save'), class: 'btn btn-primary'
    - if @rental_equipment.id.present? && can?(:destroy, @rental_equipment)
      = link_to t('rental_equipments.actions.destroy'), player_rental_equipment_path(@player, @rental_equipment), data: {method: :delete, confirm: 'Are you sure?'}, class: 'btn btn-danger'
