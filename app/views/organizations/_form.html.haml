= form_for @organization do |f|
  - if @organization.errors.any?
    #error_explanation
      %h2= "#{pluralize(@organization.errors.count, "error")} prohibited this organization from being saved:"
      %ul
        - @organization.errors.full_messages.each do |msg|
          %li= msg
  .row
    .col-xs-12
      %label.control-label= Organization.human_attribute_name(:name)
  .row
    .controls
      .col-xs-12= f.text_field :name, class: 'form-control', maxlength: 50
  .actions
    = f.submit 'Save'
