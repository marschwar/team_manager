%table.table
  %thead
    %tr
      %th= GamePants.human_attribute_name('size')
      %th= t('team_equipments.count')
  %tfoot
    %tr
      %td= t('team_equipments.sum')
      %td= pants.inject(0) { |sum, p| sum + p.count }
      %td.hidden-print
  %tbody
    - pants.each do |pants|
      %tr
        %td= pants.size
        %td= pants.count
        %td.hidden-print= link_to 'edit', edit_team_game_pants_path(pants.team, pants)
